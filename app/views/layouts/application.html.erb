<!DOCTYPE html>
<html>
  <head>
    <title>OMIS</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid">
        <div class="row flex-nowrap">
            <div class="col-md-1 p-1" style="background-color: #002C2F;">
                <div class="d-flex flex-column align-items-center align-items-sm-start px-3 pt-2 text-white min-vh-100">
                    <div class="row">
                      <div class="col p-1">
                          <img src="/assets/ic_omis.png" width="30" height="30">
                      </div>
                      <div class="col pl-1 pb-1">
                          <%= link_to "OMIS", "/", class: "fs-4 fw-bold text-white text-decoration-none d-sm-inline" %>
                      </div>
                    </div>
                    <ul class="nav nav-pills flex-column mb-sm-auto mb-0 align-items-center align-items-sm-start" id="menu">
                        <li class="nav-item">
                          <%= link_to "Home", "/", class: "nav-link text-white align-middle px-0" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to "Assets", "/business_assets", class: "nav-link text-white align-middle px-0" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to "Projects", "/projects", class: "nav-link text-white align-middle px-0" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to "Reports", "/reports/index", class: "nav-link text-white align-middle px-0" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to "Settings", "/", class: "nav-link text-white align-middle px-0" %>
                        </li>
                        <li class="nav-item">
                          <%= link_to "About", "/", class: "nav-link text-white align-middle px-0" %>
                        </li>
                    </ul>
                    <hr>
                    <div class="dropdown pb-4 pl-1">
                      <% if current_user%>
                        <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="false">
                            <span class="d-none d-sm-inline mx-1"><%= current_user.username %></span>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-dark text-small shadow">
                            <li><a class="dropdown-item" href="#">Profile</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                              <%= link_to "Sign Out", '/logout', class: "dropdown-item" %>
                            </li>
                        </ul>
                      <% else %>
                        <%= link_to "Sign In", "/user_sessions/new", class: "nav-link text-white align-middle px-0" %>
                      <% end %>
                    </div>
                </div>
            </div>
            <div class="col py-3">
              <%= yield %>
              <%= turbo_frame_tag "remote_modal", target: "_top" %>
            </div>
        </div>
    </div>
  </body>
</html>
